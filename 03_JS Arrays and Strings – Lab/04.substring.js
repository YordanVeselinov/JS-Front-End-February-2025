function substringExtraction(str, start, count) {
    let result = str.substring(start, start + count);
    console.log(result);
}


substringExtraction('ASentence', 1, 8);
substringExtraction('SkipWord', 4, 7); 